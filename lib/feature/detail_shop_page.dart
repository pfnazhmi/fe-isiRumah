import 'package:flutter/material.dart';

class DetailshopPage extends StatelessWidget {
  const DetailshopPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Detailshop Page'),
      ),
    );
  }
}
